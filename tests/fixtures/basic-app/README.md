# Basic App

This fixture app authenticates users through the auth module. Login starts in
`src/auth/login.ts`, session state is handled by `src/auth/session.ts`, and the
HTTP middleware lives in `src/server.ts`.
